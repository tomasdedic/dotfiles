extends: relaxed

ignore: |
  templates/**/*.yaml
  templates/*.yaml
  templates/

rules:
  line-length: disable
  colons: disable
  comments:
    min-spaces-from-content: 1
  octal-values:
    forbid-implicit-octal: true
  octal-values:
    forbid-explicit-octal: true

