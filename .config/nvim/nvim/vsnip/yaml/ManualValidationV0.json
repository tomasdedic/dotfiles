{
"Manual validation": {
"prefix": "adoManualValidation0",
"body": [
"- task: ManualValidation@0",
"  inputs:",
"    notifyUsers: $1  # Required  # Send a manual validation pending email to specific users (or groups). Only users with queue build permission can act on a manual validation.",
"    #instructions:  # Optional  # These instructions will be shown to the user for resuming or rejecting the manual validation. Based on these instructions the user will take an informed decision about this manual validation.",
"    #onTimeout: # Options: 'reject', 'resume' # Optional  # Reject or resume this manual validation automatically after it is pending for the specified timeout or 30 days, whichever is earlier.",
"$2"
],
"description": "[PREVIEW] Pause a pipeline run to wait for manual interaction. Works only with YAML pipelines."
}
}
