{
"Delay": {
"prefix": "adoDelay1",
"body": [
"- task: Delay@1",
"  inputs:",
"    delayForMinutes: ${1:0}  # Required  # Delay the execution of the workflow by specified time in minutes. 0 value means that workflow execution will start without delay.",
"$2"
],
"description": "Delay further execution of a workflow by a fixed time"
}
}
