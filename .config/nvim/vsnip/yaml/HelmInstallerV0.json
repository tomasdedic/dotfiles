{
"Helm tool installer": {
"prefix": "adoHelmInstaller0",
"body": [
"- task: HelmInstaller@0",
"  inputs:",
"    helmVersion: ${1:2.14.1}  # Required  # Specify the version of Helm to install",
"    #checkLatestHelmVersion:  # Optional  # Check for latest version of Helm.",
"    installKubeCtl: ${2:true}  # Required  # Install Kubectl.",
"    #kubectlVersion:  # Optional  # Specify the version of Kubectl to install",
"    #checkLatestKubeCtl:  # Optional  # Check for latest version of kubectl.",
"$3"
],
"description": "Install Helm and Kubernetes on an agent machine"
}
}
