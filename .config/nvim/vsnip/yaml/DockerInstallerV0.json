{
"Docker CLI installer": {
"prefix": "adoDockerInstaller0",
"body": [
"- task: DockerInstaller@0",
"  inputs:",
"    dockerVersion: ${1:17.09.0-ce}  # Required  # Specify the version of Docker CLI to install",
"    #releaseType: # Options: 'stable', 'edge', 'test', 'nightly' # Optional  # Pick the release type to install. Nightly is not supported on Windows",
"$2"
],
"description": "Install Docker CLI on agent machine."
}
}
