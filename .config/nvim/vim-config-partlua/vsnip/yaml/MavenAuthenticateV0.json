{
"Maven Authenticate": {
"prefix": "adoMavenAuthenticate0",
"body": [
"- task: MavenAuthenticate@0",
"  inputs:",
"    #artifactsFeeds:  # Optional ",
"    #mavenServiceConnections:  # Optional  # Credentials to use for external repositories located in the project's pom.xml.",
"$1"
],
"description": "Provides credentials for Azure Artifacts feeds and external maven repositories"
}
}
