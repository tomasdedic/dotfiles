{
"PyPI publisher": {
"prefix": "adoPyPIPublisher0",
"body": [
"- task: PyPIPublisher@0",
"  inputs:",
"    serviceEndpoint: $1  # Required  # A generic service connection for connecting to the package index.",
"    wd: $2  # Required  # The directory of the Python package to be created and published, where setup.py is present.",
"    #wheel:  # Optional  # Select whether to create and publish a universal wheel package (platform independent) in addition to an sdist package. [More information](https://packaging.python.org/tutorials/distributing-packages/#wheels).",
"$3"
],
"description": "Create and upload an sdist or wheel to a PyPI-compatible index using Twine"
}
}
